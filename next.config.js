// next.config.js - REQUIRED update for 15.5.2
const nextConfig = {
  // OLD (deprecated in 15.5.2)
  // experimental: { turbo: { /* config */ } }
  
  // NEW (required)
  turbopack: {
    // Your turbopack configuration
  }
}

export default nextConfig